name: Build iOS
description: Build the iOS app

inputs:
  filename:
    description: The filename to create
    required: true

runs:
  using: composite
  steps:
    - shell: bash
      run: flutter build ios --release --no-codesign
    - shell: bash
      working-directory: build/ios/iphoneos
      run: zip -r "../../../${{ inputs.filename }}" Runner.app
